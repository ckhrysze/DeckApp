<% content_for :head do %>
<title><%= @deck.name %></title>
<%= javascript_include_tag "jquery-1.3.2", "ba-debug.min"%>
<%= stylesheet_link_tag 'reset-fonts', 'layout'%>
<% end %>

<div id="header"></div>

<div id="container">

  <div id="deck_entry" class="column col_center">
    <div>
      Land
      <ul>
        <% @deck.maindeck.lands.each do |card| %>
          <li><%= card.name %></li>
        <% end %>
        </ul>
    </div>
    <div>
      Creatures
      <ul>
        <% @deck.maindeck.creatures.each do |card| %>
          <li><%= card.name %></li>
        <% end %>
        </ul>
    </div>
    <div>
      Spells
      <ul>
        <% @deck.maindeck.spells.each do |card| %>
          <li><%= card.name %></li>
        <% end %>
        </ul>
    </div>
  </div>

  <div id="deck_list" class="column col_left">
  </div>

  <div id="deck_stats" class="column col_right">
  </div>
</div>

<div id="footer-wrapper">
  <div id="footer"></div>
</div>
